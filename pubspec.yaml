name: any_borders
description: Flexible decoration and border primitives with per-side fills, inner/outer radii, and merged background-border painting.
version: 0.0.1
publish_to: none

environment:
  sdk: '>=3.3.0 <4.0.0'
  flutter: '>=3.22.0'

dependencies:
  flutter:
    sdk: flutter
