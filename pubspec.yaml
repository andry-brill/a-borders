name: any_borders
description: Universal border box decoration with non-uniform border, custom corners and merged background-border painting
version: 0.0.1
publish_to: none

environment:
  sdk: '>=3.3.0 <4.0.0'
  flutter: '>=3.22.0'

dependencies:
  flutter:
    sdk: flutter
  vector_math: ^2.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  test: ^1.30.0
  flutter_lints: ^6.0.0

